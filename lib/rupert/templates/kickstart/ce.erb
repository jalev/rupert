install
text
reboot
lang en_US.UTF-8
keyboard us
network --onboot yes --device eth0 --bootproto dhcp --noipv6 --hostname tester.rupert.io
rootpw  --iscrypted $6$4wpFaSxuqIexTWRc$WTg84vAikAIJvfGQQB/j8PZFYcucVNj0wgBTcuLyjgQkfSVEYHWfN1QHqEmCHPwSL7g.Lh5zZuGSQM.fzNvro0
firewall --service=ssh
authconfig --enableshadow --passalgo=sha512
selinux --enforcing
timezone --utc America/New_York
bootloader --location=mbr --driveorder=vda --append="crashkernel=auto rhgb quiet"
# The following is the partition information you requested
# Note that any partitions you deleted are not expressed
# here so unless you clear all partitions first, this is
# not guaranteed to work
clearpart --all --initlabel 
autopart

%packages
@core
@server-policy
yum 
ntp
wget
dhclient
%end

%post
su -c 'rpm -Uvh http://dl.fedoraproject.org/pub/epel/6/x86_64/epel-release-6-8.noarch.rpm'
su -c 'echo "192.168.0.8   rupert.provisioner.io" >> /etc/hosts'

yum -t -y -e 0 update
yum -t -y -e 0 install puppet
/usr/bin/puppet agent --config /etc/puppet/puppet.conf -o --tags no_such_tag --server rupert.provisioner.io  --no-daemonize


